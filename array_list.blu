ArrayList := |T: type| struct {
  len: uint,
  data: []{T | Uninit(T)},

  Self := #this

  is_empty := |self: Self| self.len == 0
  
  as_slice := |self: Self| {
    s := data[0..self.len]
    assume s is []T
    s
  }
}
