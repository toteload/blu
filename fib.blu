fib: fn(i32) i32 = fn(n: i32) i32 {
  if n <= 1 {
    return 1
  } else {
    return fib(n-1) + fib(n-2)
  }
}
 
main: fn() i32 = fn() i32 {
  i: i32 = 1
  x: i32 = 2 + 5 * 16 / 4 * 35 + i

  s: i32 = fib(x)

  while 1 == 1 {
    if i + 2 * 3 == 10 {
      break
    }

    i = i+1

    continue
  }

  return fib(10)
}
