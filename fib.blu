fib :: (n: i32): i32 {
  if n <= 1 {
    return 1;
  } else {
    return fib(n-1) + fib(n-2);
  }
}

main :: (): i32 { 
  i : i32 = 0;

  while 1 {
    if i + 2 * 3 == 10 and 1 {
      break;
    }

    i = i+1;
    continue;
  }

  return fib(10);
}
