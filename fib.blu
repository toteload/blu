fib :: (n: i32): i32 -> {
  if n <= 1 {
    return 1;
  } else {
    return fib(n-1) + fib(n-2);
  }
}

main :: (): i32 -> { 
  i : i32 = 0;
  while i <= 10 {
    i = i+1;
  }
  return fib(10);
}
