mod main

main := ||: i32 {
  s: []u8 = "7"
  i: usize = 0

  // A [1]T unifies with T, so a one-character string literal compares as its element.
  #debug(s[i] == "7")
  #debug(s[i] >= "0")
  #debug(s[i] <= "9")

  // The same rule as a plain coercion.
  c: u8 = "A"
  #debug(c)

  0
}
