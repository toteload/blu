main : (): i32 = || {
  a : i32 = 2
  b : i32 = 3
  c : i32 = a + b
  d : i32 = b - a
  e : i32 = a * b
  { c + d } / e - 1
}

; %a = alloc (i32)
; store %a, 2
; %b = alloc (i32)
; store %b, 3
; %c = alloc (i32)
; %0 = load %a
; %1 = load %b
; %2 = add %0, %1
; store %c, %2
; %d = alloc (i32)
; %3 = load %a
; %4 = load %b
; %5 = add %3, %4
; store %d, %5
; %e = alloc (i32)
; %6 = load %a
; %7 = load %b
; %8 = add %6, %7
; store %e, %8
