mod main

main : () i32 = || {
  x comptime := as(i32) {45 + 5} / 5
  a : i32 = 0
  b : i32 = one_or_two(false)
  c : i32 = one_or_two(true)
  comptime fac(x) - 3628800
}

fac := |n: i32|: i32 {
  if n == 0 {
    1
  } else {
    n * fac(n-1)
  }
}
