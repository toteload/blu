main : (): i32 = || {
  count(.{ 1, 2, 3, 4, 5, }, |x| { x % 2 == 1 }) - 3
}

count : ([]i32, (i32): bool): i32 = |xs, pred| {
  s : i32 = 0
  for xs do x {
    if pred(x) {
      s = s + 1
    }
  }

  s
}
