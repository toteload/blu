fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

fib : (i32): i32 = |n| {
  if n <= 2 {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}
